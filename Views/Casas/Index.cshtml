@model IEnumerable<ARSAN_Web.Models.Casa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Direccion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPropietario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Direccion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPropietario)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdCasa">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdCasa">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdCasa">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
