@model IEnumerable<ARSAN_Web.Models.JuntaDirectiva>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AnioInicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AnioFin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cluster)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AnioInicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AnioFin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cluster.Nombre)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdJuntaDirectiva">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdJuntaDirectiva">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdJuntaDirectiva">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
